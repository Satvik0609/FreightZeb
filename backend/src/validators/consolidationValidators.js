// Consolidation currently does not require user input.
// Shipments existence is validated in service layer against database state.
const consolidateShipmentsRules = [];

module.exports = { consolidateShipmentsRules };
