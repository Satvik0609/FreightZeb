# ── Node.js ───────────────────────────────────────────────────────────────────
node_modules/
dist/
build/
npm-debug.log*
yarn-debug.log*

# ── Environment files (NEVER commit real secrets) ─────────────────────────────
.env
.env.local
.env.production
# .env.example is intentionally kept — it's a template with no real secrets

# ── Python ────────────────────────────────────────────────────────────────────
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.so
*.egg
*.egg-info/
.venv/
venv/
ENV/
.pytest_cache/
.coverage
htmlcov/
catboost_info/

# ── Kaggle datasets — too large for git (3GB+) ────────────────────────────────
# Re-download with: cd ml/service && python scripts/download_datasets.py
ml/service/data/

# ── Large model files — too big for GitHub (88MB) ────────────────────────────
# Retrain with: cd ml/service && python scripts/retrain_all.py
ml/service/saved_models/fuel_estimator_v2.joblib

# ── OS / Editor ───────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*.swo
*~

# ── Uploads (user-generated, not source code) ─────────────────────────────────
backend/uploads/avatars/
backend/uploads/proof-of-delivery/
backend/uploads/documents/

# ── Logs ──────────────────────────────────────────────────────────────────────
backend/logs/
*.log
